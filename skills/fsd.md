# FSD Skill

**Layers (↓ deps only):**
app → pages → widgets → features → entities → shared/ui/shadcn/

**Rules:**

- widgets/ = domain UI slices (model/ui)
- features/ = user actions (model/ui/lib)
- shared/ = shadcn primitives + api/utils only
- New feature → duplicate gif-search/ → rename

**File paths MUST match ARCHITECTURE.md table**
