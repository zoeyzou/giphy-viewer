# React + Shadcn Skill

**Patterns:**

- Hooks → model/ only
- Zod validation → lib/
- Vitest + RTL for all tests
- Tailwind responsive: grid-cols-1 md:grid-cols-3

**Giphy API:**

```
https://api.giphy.com/v1/gifs/search?q={query}&offset={offset}&limit=3&rating=g&api_key={key}
```
